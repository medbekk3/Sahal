import { ar } from "@/lib/i18n/ar";
import {
  formatCurrencyAr,
  formatDateAr,
  formatNumberAr,
  mapFirebaseErrorAr,
  mapStatusLabelAr,
} from "@/lib/i18n/format";

/** Driver dashboard copy — always Arabic. */
export const driverAr = ar.driver;

export {
  formatCurrencyAr,
  formatDateAr,
  formatNumberAr,
  mapFirebaseErrorAr,
  mapStatusLabelAr,
};

/** Wrapper for RTL-safe driver sections. */
export const driverRtlClass = "dir-rtl text-right";
